# local MLflow fallback store (used when the docker MLflow server is down)
mlruns/
mlruns.db
mlartifacts/

# prebaked live job ids (machine-specific; created by `make prebake`)
.prebaked-jobs.json

# provider id mapping discovered at preflight (cluster-specific)
.provider-map.json

# live MaaS endpoint config -- holds the API key, never commit (use .env.example)
.env

# jupyter
.ipynb_checkpoints/

# python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
build/
dist/
uv.lock
